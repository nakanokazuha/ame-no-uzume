{
  "name": "ame-no-uzume",
  "private": true,
  "packageManager": "pnpm@10.12.4",
  "scripts": {
    "dev:web": "pnpm --dir apps/web dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "pngjs": "^7.0.0"
  }
}
